;显示A-Z26个大写英文字母
DATA SEGMENT
	LETTER DB 'A'
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE, DS:DATA
START:
	MOV AX, DATA
	MOV DS, AX
	MOV CX, 26
	LEA SI, LETTER
NEXT:
	MOV DL, [SI]
	MOV AH, 02H
	INT 21H
	INC LETTER
	LOOP NEXT
EXIT:
	MOV AH, 4CH
	INT 21H
CODE ENDS
	END START